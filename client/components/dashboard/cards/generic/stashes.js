const React = require('react');

const GenericCardStashes = React.createClass({
    render() {
        return (
            <div>stashes</div>
        );
    }
});

module.exports = GenericCardStashes;
