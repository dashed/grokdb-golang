const React = require('react');
const orwell = require('orwell');


const Cards = React.createClass({
    render() {
        return (
            <div>cards</div>
        );
    }
});

module.exports = orwell(Cards, {});
